name: weather-app

services:
  app:
    image: michael0221/weather-app:ec2 # Built & pushed via GitHub Actions
    ports:
      - "8000:8080" # 8000 server port -> 8080 container port
    restart: unless-stopped # Stop on errors, restart on clean exits